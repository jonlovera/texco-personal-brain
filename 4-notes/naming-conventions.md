# Naming Conventions

> Keep names consistent so notes, links, and the Obsidian graph resolve to one node (Obsidian
> matches links by exact name — a variant spelling becomes a separate, disconnected node).

## Ticket prefixes
- `TT-123` → TexTrack (see [[textrack]])
- `TXI-45` → TaxiSoft (see [[taxisoft]])
- (payroll / texco-tools prefixes — TBC; add when known)

## Canonical spellings (use these everywhere)
- **JobPac** — the external ERP / job-costing system (not "JobPack" / "Jobpack"). See [[audit]].
- **TaxiSoft** — the AP/OCR system (repo `AccountsPayable`) (not "TexTaxi"). See [[taxisoft]].
- **Brock** — one of the two app owners (not "Brok"). See [[people]].
- **HiBob / "Bob"** — the external HR system. See [[audit]].
